```
ITERATION 0 SCORES:
  quality: 4/10 — The artifact is structurally sound with appropriate markdown headings, a bullet list, and readable prose, but the content is entirely self-referential and hollow — it describes what an artifact should contain rather than actually containing it. The "Contents" section enumerates features the document claims to have, and the "domain-specific content" placeholder remains empty, making this more a skeleton than a real artifact. To raise this score, replace the self-referential meta-commentary with actual substantive content in the domain the artifact is meant to serve — the "placeholder section" must become a real section with real information.
COMPOSITE: 4.0/10

ASI (highest-leverage direction):
The entire document describes itself rather than being a thing. The single highest-leverage fix is to replace the self-referential content with substantive domain material: give the artifact a real purpose, a real domain, and real information in its body. Specifically, replace the "Contents" bullet list (which currently just narrates structural features) and the empty "domain-specific content" placeholder with actual prose or data about a concrete subject. A document that says "this document has clear structure" instead of demonstrating it scores near the floor on quality regardless of formatting.
```