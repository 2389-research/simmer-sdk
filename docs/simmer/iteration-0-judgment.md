```
ITERATION 0 SCORES:
  quality: 4/10 — The artifact has a clean heading hierarchy and is honest about being a test baseline, but the "Contents" section lists three labeled bullets (Section 1, Section 2, Section 3) that point to no actual sections — they are self-referential placeholders for content that doesn't exist. The document describes its own structure rather than demonstrating it. Structure exists; substance does not. — Replace the three placeholder bullets in the Contents section with actual prose content under real `##` subheadings, so the document demonstrates the quality being evaluated rather than just promising it.
COMPOSITE: 4.0/10

ASI (highest-leverage direction):
The Contents section is entirely hollow — it names three sections but provides no actual content beneath them. The single highest-leverage change is to expand each placeholder bullet into a real `## Section N` subheading with 2–3 substantive sentences, so the document contains genuine information rather than being a self-description of a skeleton. This would move the artifact from a structural outline to a coherent document worthy of quality assessment.
```